# GAOL Frontend

GAOL uses React + Vite for it's web interface. The frontend scripts are contained under `client/src`.
Package Dependencies are as follows:  
- React/Vite baseline packages
- SamJS (TTS Package)

Setup from the client directory:
```
npm install
npm run dev
```
